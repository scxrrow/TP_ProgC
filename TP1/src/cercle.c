#include <stdio.h>
#include <math.h>

int main(){
    double pi = 3.14159;
    double r = 6.0;

    printf("l'air du cercle est : %f", pi * r * r);
    printf("le perimetre du cercle est : %f", 2 * pi * r);
}
